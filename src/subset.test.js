import { subset }  from './subset';

describe('subset', () => {
  test.todo('has'); // has(predicate, object?) filters current set
  test.todo('out'); // out(predicate, keep?) follows predicate
  test.todo('in'); // in(predicate, keep?) follows predicate in the other direction
  test.todo('addIn'); // addIn(subject, predicate) adds triples
  test.todo('addOut'); // addOut(predicate, object) adds triples
  test.todo('values'); //
});
